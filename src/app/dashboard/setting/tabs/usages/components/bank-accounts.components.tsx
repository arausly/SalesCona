import { Separator } from "@components/ui/separator";
import React from "react";

export const StoreBankAccount = () => {
    return (
        <section>
            <p className="text-lg font-semibold">Bank Account</p>
            <Separator className="mb-2" />
        </section>
    );
};
